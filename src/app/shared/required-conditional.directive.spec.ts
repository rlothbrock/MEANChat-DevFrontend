import { RequiredConditionalDirective } from './required-conditional.directive';

describe('RequiredConditionalDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredConditionalDirective();
    expect(directive).toBeTruthy();
  });
});
